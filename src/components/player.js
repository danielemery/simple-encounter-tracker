import React from 'react';
import { Segment } from 'semantic-ui-react';

const Player = () => {
    return (
        <Segment basic>
            <h1>Player</h1>
            Welcome, Player
        </Segment>
    );
}

export default Player;